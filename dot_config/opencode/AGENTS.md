DO NOT run bun typecheck automatically in the opencode repo because it eats my cpu like crazy, if user encourages u to start using it again feel free, otherwise avoid it

When you git push in opencode repo, use `git push --no-verify`
